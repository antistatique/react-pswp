import PhotoSwipeWrapper from './PhotoSwipeWrapper';

describe('PhotoSwipeWrapper', () => {
  it('should be defined', () => {
    expect(PhotoSwipeWrapper).toBeDefined();
  });

  // it('should update hooks', () => {
  //   wrapper.simulate('click');
  // });
});
