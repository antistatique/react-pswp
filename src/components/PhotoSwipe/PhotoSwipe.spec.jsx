import PhotoSwipe from './PhotoSwipe';

describe('PhotoSwipe', () => {
  it('should be defined', () => {
    expect(PhotoSwipe).toBeDefined();
  });

  // it('should update hooks', () => {
  //   wrapper.simulate('click');
  // });
});
